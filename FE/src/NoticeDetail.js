function NoticeDetail({ id, title, updatedAt }) {
  return (
    <div>
      <p>id: {id}</p>
      <p>title: {title}</p>
      <p>updatedAt: {updatedAt}</p>
    </div>
  );
}

export default NoticeDetail;
